name: deovibase
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  cloud_firestore: ^5.4.1
  cloud_firestore_web: ^4.3.0
  firebase_core: ^3.4.1
  firebase_database: ^11.1.2
  firebase_storage: ^12.3.0
  flutter:
    sdk: flutter
  flutter_webrtc: ^0.11.7
  image: ^4.2.0
  image_picker: ^1.1.2
  mobile_scanner: ^5.2.3
  permission_handler: ^11.3.1
  qr_code_tools: ^0.1.0
  url_launcher: ^6.3.0
  video_player: ^2.9.1
  web_socket_channel: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
